import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.oduold.mercy',
  appName: 'mercy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
